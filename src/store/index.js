import { createStore } from 'vuex';
import getInfo from '@/store/getInfo'

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        getInfo,
    }
})

